// Configuration for builds
const config = {
	VERSION: '4.0.1', // Version of the build
	CHANGE_NAME: 'nightfall BL -- fix border-radius not being circle for candidate-clip', // Change name for debug builds, empty for production
	// BUILD_FOLDER: 'debug', // Default build folder, can be 'debug' or 'prod'
}

module.exports = config
