// Configuration for builds
const config = {
	VERSION: '5.0.1-debug', // Version of the build
	CHANGE_NAME: 'smartbar--adjust font sizes and inheritance for better consistency', // Change name for debug builds, empty for production
	// BUILD_FOLDER: 'debug', // Default build folder, can be 'debug' or 'prod'
}

module.exports = config
