// Configuration for builds
const config = {
	VERSION: '5.1.0-debug', // Version of the build
	CHANGE_NAME: 'improve clipboard history styling and refactor element names', // Change name for debug builds, empty for production
	// BUILD_FOLDER: 'debug', // Default build folder, can be 'debug' or 'prod'
}

module.exports = config
