// Configuration for builds
const config = {
	VERSION: '5.0.1-debug', // Version of the build
	CHANGE_NAME: 'Rewrite action tiles to match Gboard style with new SnyggV2 props', // Change name for debug builds, empty for production
	// BUILD_FOLDER: 'debug', // Default build folder, can be 'debug' or 'prod'
}

module.exports = config
