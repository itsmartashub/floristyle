// Configuration for builds
const config = {
	VERSION: '5.0.1', // Version of the build
	CHANGE_NAME: '', // Change name for debug builds, empty for production
	// BUILD_FOLDER: 'debug', // Default build folder, can be 'debug' or 'prod'
}

module.exports = config
